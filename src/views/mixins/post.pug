mixin post(info)
    div
            h4 
                a(href=`/posts/${info.id}`)=info.title 
            ul
                li 조회수 #{info.views}
                li 댓글 #{info.comments }
                li #{info.createdAt}