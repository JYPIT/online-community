extends base.pug

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="title" placeholder="제목" type="text" required)
        input(name="content" placeholder="내용" type="text" required)
        input(name="hashtags" placeholder="해시태그 (콤마로 구분)" type="text")
        input(type="submit" value="업로드")